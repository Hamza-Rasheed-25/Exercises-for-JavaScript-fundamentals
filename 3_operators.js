// Rewrite this if-else as a ternary operator:
// let status;
// if (age >= 18) status = "Adult";
// else status = "Minor";


// Write a line using && that only runs a function logError() if a variable isError is true.


// Use || to assign a default name "Guest" to a variable user if inputName is empty string or null.


// Write a function that returns true if a number is even and false if odd using % .


// Calculate 2 to the power of 10 using the ** operator.


// Unary Plus: Use the unary + operator to convert the boolean true into the number 1.


// What does console.log(null > 0) vs console.log(null >= 0) print? (Advanced coercion check).


// Use += to add 5 to a variable score that starts at 10 .


// Use ! to check if a variable isLoaded is not true.


// Predict the output of [] == 0 vs [] === 0.