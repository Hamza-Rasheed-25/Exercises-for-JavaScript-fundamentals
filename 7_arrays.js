// Given ['a', 'b', 'c', 'd'], use splice to remove 'b' and 'c'.


// Use filter to return only even numbers from [1, 2, 3, 4, 5, 6] .


// Use map to convert ['apple', 'banana'] into ['APPLE', 'BANANA'] .


// Given an array of users [{id:1}, {id:2}], use find to get the user with id: 2.


// Use reduce to multiply all numbers in [2, 3, 4] (Product).


// Combine arr1 = [1, 2] and arr2 = [3, 4] into a new array using the spread operator.


// Extract the first and third elements of [10, 20, 30] into variables first and third.


// Correctly sort [10, 5, 20, 2] numerically (not alphabetically).


// Use a method to check if [1, 2, 3] contains the number 2 (using includes or some).


// create a shallow copy of an array original using slice().