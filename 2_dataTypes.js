// Write a function that accepts a value and returns "Array" if it's an array, "Null" if it's null, or the result of typeof otherwise (fixing the typeof bugs) .


// What is the result of console.log("10" - 2) vs console.log("10" + 2)? .


// Create a BigInt variable storing a number larger than 9007199254740991 and log its type .


// Convert the string "123" to a number using two different methods (e.g., Number() and unary +) .


// Write a conditional check that fails for 0 == false but passes for 0 === 0 .


// Declare a variable a without assigning it, and a variable b assigned to null. Log them both .


// Create a string that includes a variable name inside it using backticks (if covered in broader JS context) or concatenation as shown in.


// Write code that generates NaN (e.g., undefined + 1) and verify it using typeof .


// Use the Boolean() function to check if the string "0" is truthy or falsy .


// Create an array arr1 = [1, 2]. Set arr2 = arr1. Push 3 to arr2. Log arr1. What changed?.