// Create a switch statement that takes a string "Mon", "Tue", etc., and logs "Weekday", or logs "Weekend" for "Sat"/"Sun" .


// Write a switch case without break statements. Call it with a value that matches the first case and observe how many subsequent cases execute.


// Write logic to categorize a person's BMI: <18.5 (Underweight), 18.5-24.9 (Normal), >25 (Overweight) .


// Refactor a function processUser(user) to return immediately if user is null, avoiding a nested if .


// Write an if statement that runs if an array items has elements (check items.length).


// Write code with a nested if to check if isLoggedIn is true, and inside that, if hasPremium is true.


// Chain two ternary operators to check if a number is positive, negative, or zero.


// Why does if (x = 10) always run? Fix it to use comparison.


// Write a function where code is placed after a return statement. Does it run?


// Write an if condition using () to group OR and AND logic: e.g., "if (age > 18 OR hasParent) AND hasTicket".